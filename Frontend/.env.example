# Leave empty when using Vite dev proxy or nginx /api reverse proxy
VITE_API_BASE_URL=

# Or point directly at the API (e.g. production without reverse proxy):
# VITE_API_BASE_URL=http://localhost:8000
